# Railway deployment – variable templates for each service
# Railway can suggest these variables; add the block for each service in that service’s Variables tab.
# For DATABASE_URL / REDIS_URL: use "Add Reference" → Postgres or Redis, then for API’s DATABASE_URL change prefix to postgresql+asyncpg://

# ============ API service ============
# Copy this block into the API service Variables (RAW Editor or add one-by-one).
# Replace DB/Redis placeholders with References to your Postgres and Redis services.
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/syllascribe
DATABASE_URL_SYNC=postgresql://user:password@host:5432/syllascribe
REDIS_URL=redis://default:password@host:6379
UPLOAD_DIR=/app/data/uploads
ALLOWED_ORIGINS=https://your-web-service.up.railway.app
PORT=8000
USE_LLM_CLASSIFIER=false
LLM_PROVIDER=openai
LLM_API_KEY=
UPLOAD_RETENTION_HOURS=168

# ============ Worker service ============
# Copy this block into the Worker service Variables.
DATABASE_URL_SYNC=postgresql://user:password@host:5432/syllascribe
REDIS_URL=redis://default:password@host:6379
UPLOAD_DIR=/app/data/uploads
USE_LLM_CLASSIFIER=false
LLM_PROVIDER=openai
LLM_API_KEY=

# ============ Web service ============
# Copy this block into the Web service Variables.
# Set NEXT_PUBLIC_API_BASE_URL to your API service’s public URL (e.g. https://your-api-service.up.railway.app).
NEXT_PUBLIC_API_BASE_URL=https://your-api-service.up.railway.app
PORT=3000
